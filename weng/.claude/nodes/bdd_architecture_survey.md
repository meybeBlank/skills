流程节点：BDD 项目架构勘察
描述: 以只读方式探测目标项目的真实技术栈、分层结构、命名约定、测试框架与目录、依赖注入方式，并定位同类功能的参考实现，产出一份架构基线结果包。后续验证矩阵、契约、测试、代码四个阶段的技术选型必须以此为准，确保产出符合项目已有架构。

# 项目架构勘察阶段

## 系统指令

你现处于**项目架构勘察阶段**，以自动流程节点身份，通过**只读探测**（读取文件、搜索、列目录）摸清目标项目的真实架构，产出一份架构基线结果包。

本阶段**不编写任何业务代码、不修改任何文件**，只观察、归纳、记录。

输入：需求理解结果包（用于圈定勘察范围——本次功能可能落在哪些模块）。

任务：回答一个核心问题——「本次功能的代码、测试、接口，应该按什么技术栈、放在哪些目录、遵循什么命名和分层规范来写，才能与项目现有代码融为一体？」

## 核心原则

1. **一切从实际出发**：所有结论必须来自对项目文件的真实探测，严禁凭经验臆断技术栈或目录结构。
2. **无证据不下结论**：每项结论都要能指向具体的证据文件（配置文件、现有源码、目录路径）。
3. **以既有为准**：同类实现已有先例时，优先复用既有模式，而非引入新范式。
4. **空项目显式声明**：若项目为空或无同类实现，明确标注「无先例」，并在结果包中列出建议采用的技术选型供用户确认。

## 步骤 1：技术栈识别

从项目依赖与构建配置文件识别技术栈。

### 1.1 定位构建/依赖文件

按语言生态查找并读取项目根及子模块的构建配置，例如：`pom.xml`、`build.gradle`、`package.json`、`go.mod`、`requirements.txt`、`pyproject.toml`、`Cargo.toml`、`*.csproj` 等。

### 1.2 归纳技术栈

从依赖清单中提取：

- **主语言与版本**
- **应用框架**（Web 框架、服务框架等）
- **持久化/数据访问方式**（ORM、查询构建器、原生驱动）
- **依赖管理与构建工具**
- **既有的关键第三方库**

**自我检查：**

- [ ] 构建/依赖文件已定位并读取
- [ ] 主语言、框架、数据访问方式均有明确来源
- [ ] 无凭空推断的技术栈

## 步骤 2：分层与目录结构

### 2.1 探测源码目录结构

列出项目源码目录树（聚焦与本次功能相关的模块），识别分层方式：

- 是否有明确的分层（如 接口层 / 应用层 / 领域层 / 基础设施层，或 controller / service / repository 等）
- 每层对应的目录位置与包/命名空间路径
- 模块之间的组织方式（按功能划分 / 按类型划分）

### 2.2 定位新代码落点

结合需求，判断本次功能的各层代码应新增到哪些具体目录。若需新建模块，说明应遵循的既有目录约定。

**自我检查：**

- [ ] 已列出相关源码目录结构
- [ ] 分层方式有现有代码作为证据
- [ ] 本次功能各层的落点目录已明确

## 步骤 3：命名与编码约定

从现有源码中归纳项目实际遵循的约定（以既有代码为准，而非通用规范）：

- **命名风格**：类/类型、方法/函数、变量、常量、文件命名的实际风格
- **接口/契约命名**：接口、DTO/VO、请求响应对象的命名模式
- **错误处理约定**：统一异常类型、错误码/错误响应结构、日志方式
- **依赖注入/装配方式**：如何获取依赖（构造注入、容器、工厂、手动装配等）

每项约定附至少一个证据文件路径。

**自我检查：**

- [ ] 命名风格有现有代码佐证
- [ ] 错误处理约定已识别
- [ ] 依赖注入/装配方式已识别

## 步骤 4：测试框架与约定

这一步直接决定测试用例开发（红）节点怎么写测试。

### 4.1 测试框架识别

从依赖和现有测试代码识别：

- **功能/端到端测试**：是否有 BDD 框架（如 Cucumber/Behave/SpecFlow 等）或其他 E2E 方案；`.feature` 文件放在哪里；步骤定义放在哪里
- **集成/接口测试**：使用什么框架与断言方式；如何隔离外部依赖（Mock/内存实现/容器）
- **单元测试**：测试框架、Mock 库、断言库

### 4.2 测试目录与命名约定

- 各层测试代码的目录位置
- 测试类/测试方法/测试文件的命名约定
- 场景 ID 的标注方式（如何把测试关联到需求场景——标签、注解、注释等既有做法）
- 运行测试的命令（从构建配置或脚本中提取）

若项目无任何测试先例，标注「无测试先例」，并给出与技术栈匹配的建议测试方案供用户确认。

**自我检查：**

- [ ] 三层测试各自的框架已识别（或标注无先例）
- [ ] 测试目录与命名约定有证据
- [ ] 场景 ID 标注方式已明确
- [ ] 测试运行命令已提取

## 步骤 5：同类实现参考

在项目中查找与本次功能相似的既有功能（相似的接口、相似的业务动作、相似的数据流），作为本次开发的直接模仿对象。

- 定位 1~3 个最相近的既有功能实现
- 记录其从接口到持久化的完整文件清单（各层代表文件路径）
- 归纳其可复用的模式（分层调用方式、错误处理、测试写法）

若无同类实现，标注「无同类先例」。

**自我检查：**

- [ ] 已定位同类实现（或标注无先例）
- [ ] 参考实现的分层文件清单完整
- [ ] 可复用模式已归纳

## 步骤 6：本次改动落点（改动型需求）

若需求理解结果包的存量变更清单表明本次为**改动型需求**，除步骤 2 的"新代码落点"外，还须只读定位本次要**修改**的既有代码与既有测试，供下游改动型红灯、回归范围直接使用。新建型需求标注"新建型需求，无既有改动落点"，跳过本步骤。

### 6.0 模式回写（以真实代码为准的最终裁定）

需求理解阶段的模式判定基于需求描述，可能不准（例如"新增功能"实为在既有链路上加分支）。架构勘察基于真实代码探测，是模式判定的**最终裁定者**。执行本步骤前先做模式核对：

1. 若需求包判为**新建型**，但本步骤探测发现本次功能**需要修改任何既有源文件、接入既有调用链、或复用既有能力** → 判定为"模式误判"，**显式回写修正为改动型（含混合）**，并以架构勘察结论为准。
2. 回写时输出一条明确记录：`模式回写：需求阶段判定为"新建型"，架构勘察发现需改动既有文件 {文件列表}，修正为"改动型（混合）"。下游一律以此为准。`
3. 回写后，本步骤补齐既有代码/测试改动清单；下游验证矩阵、测试、代码阶段以架构勘察的改动型结论为准，忽略需求包中过时的"新建型"标注。
4. 反向一致时（需求包已判改动型），无需回写，直接进行下面的落点定位。

### 6.1 既有代码改动清单

结合存量变更清单，逐条定位受影响的既有源文件：

```
| 存量变更项 | 变更类型 | 既有源文件路径 | 涉及的类/方法 | 改动性质 |
|-----------|---------|-------------|-------------|---------|
| ... | 修改 | src/.../XxxService.ext | XxxService.doYyy() | 修改方法体 |
| ... | 废弃 | src/.../ZzzHandler.ext | ZzzHandler.old() | 移除逻辑 |
```

### 6.2 既有测试改动清单

定位与上述改动关联的既有测试文件（哪些需改断言、哪些作废、哪些纳入回归但不改）：

```
| 既有测试文件路径 | 关联场景ID | 处理方式 |
|---------------|-----------|---------|
| test/.../XxxServiceTest.ext | PW-RESET-03 | 修改断言为新预期 |
| test/.../ZzzHandlerTest.ext | PW-OLD-02 | 作废 |
| test/.../AaaTest.ext | PW-RESET-01 | 回归，不修改 |
```

**自我检查：**

- [ ] 已执行模式回写核对：需求包判新建型但实际需改既有文件时，已显式修正为改动型并留记录
- [ ] 已判定新建型/改动型（以架构勘察真实探测为最终准绳）
- [ ] 改动型的既有源文件改动清单有真实文件路径佐证
- [ ] 既有测试改动清单区分了修改/作废/回归
- [ ] 改动落点与需求理解存量变更清单逐条对应，无遗漏

## 最终输出：架构基线结果包

完成所有步骤后，汇总为一份完整输出：

```markdown
# 架构基线结果包

## 1. 技术栈
- 主语言/版本：{...}（证据：{文件}）
- 应用框架：{...}（证据：{文件}）
- 数据访问：{...}（证据：{文件}）
- 构建/依赖工具：{...}
- 关键第三方库：{...}

## 2. 分层与目录结构
- 分层方式：{...}
- 各层目录/包路径：
  - {层}：{路径}（证据：{现有文件}）
- 本次功能各层落点：
  - {层}：{目标目录}

## 3. 命名与编码约定
- 命名风格：{...}（证据：{文件}）
- 接口/DTO 命名：{...}
- 错误处理约定：{...}（统一异常/错误码/日志）
- 依赖注入/装配方式：{...}

## 4. 测试框架与约定
- 功能/E2E 测试：{框架 + 目录 + 命名}（或「无先例，建议：...」）
- 集成/接口测试：{框架 + 隔离方式 + 目录}
- 单元测试：{框架 + Mock/断言库 + 目录}
- 场景 ID 标注方式：{...}
- 测试运行命令：{...}

## 5. 同类实现参考
- 参考功能：{名称}
  - 分层文件清单：{各层路径}
  - 可复用模式：{...}
（无则标注「无同类先例」）

## 6. 本次改动落点（改动型需求）
{新建型标注"新建型需求，无既有改动落点"；改动型列出既有代码改动清单与既有测试改动清单}

## 7. 待确认事项
{汇总所有「无先例」的建议选型、以及存在多种既有模式无法自我裁定的分歧，供用户确认}
```

## 质量门禁

| 检查项 | 通过标准 |
|--------|---------|
| 技术栈 | 主语言、框架、数据访问方式均有证据文件 |
| 分层落点 | 本次功能各层的目标目录明确 |
| 命名约定 | 命名/错误处理/依赖注入均有现有代码佐证 |
| 测试基线 | 三层测试框架、目录、命名、运行命令已明确（或无先例已标注并给出建议） |
| 同类参考 | 已定位同类实现或明确标注无先例 |
| 改动落点 | 改动型需求已定位既有源文件与既有测试改动清单（新建型已标注无改动落点） |
| 无臆断 | 所有结论可指向证据文件，无凭空推断 |

**最终判断信号**：拿着这份基线，契约/测试/代码三个阶段能明确知道用什么技术栈、放到哪个目录、按什么命名和分层来写。能，则本阶段完成。

**流转前的用户确认（条件性）**：若"待确认事项"非空——存在"无先例"的建议选型，或多种既有模式无法自我裁定的分歧——必须先向用户展示这些待确认项并获得确认，再流转到下一阶段：验证矩阵。因为架构基线的技术选型将被契约、测试、代码全部下游阶段直接采纳，未经确认的建议选型不得默认生效。若"待确认事项"为空（全部结论有证据、无分歧），则自动流转，无需确认。
