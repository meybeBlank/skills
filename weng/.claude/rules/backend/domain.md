---
paths:
  - "backend/src/domain/**"
---
# 领域层（Domain）编码规范

> 本文件规定后端领域层的编码约束。领域层为纯业务层，不含任何技术实现。

## 导入边界

- **严禁**导入 `fastapi`、`sqlalchemy`、`jose`、`jwt`、`pydantic` 等任何第三方库
- 仅限使用 Python 标准库

## 业务规则

- 金额约束：非负数，两位小数，禁止负金额入库（由 `Money` 值对象负责校验）
- 领域层只包含纯业务实体、值对象、业务异常，不含任何技术实现

