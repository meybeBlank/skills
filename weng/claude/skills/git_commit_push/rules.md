# 项目禁止提交文件清单

> 本文件列出本项目禁止提交到 git 仓库的文件。执行 `git add` / `commit` 时加载。

## 禁止提交的文件

- `.env`（含真实密钥）— 只提交 `.env.example`
- `family.db`（运行时生成的数据库文件）
- `node_modules/`
- `__pycache__/`
- `.venv/`
- 任何含密钥/凭证的文件（API 密钥、私钥、访问令牌等）

## 判断标准

如果文件包含真实的敏感信息或可被利用的凭证，禁止提交。如有疑问，先询问用户。

